.DS_Store
.env
# No trailing slash — a trailing-slash pattern only matches a real
# directory, not a symlink named node_modules (e.g. one linked in from a
# git worktree for local testing), which would otherwise slip past this
# rule and get accidentally committed.
/node_modules

# React Router
/.react-router/
/build/
